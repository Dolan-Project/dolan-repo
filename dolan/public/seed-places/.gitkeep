# Seeded destination photos (optional).
# Generated by `npm run db:seed` / `npm run db:seed:photos` when GOOGLE_MAPS_SERVER_KEY is set.
# Without local files, seeders fall back to permanent Unsplash URLs in places.cached_photo_url.
